mod tasks;

use tasks::{TaskItem, TaskList};

fn main() {
    // let mut tl = TaskList::new("New List");
    // let mut c = TaskItem::new("Test", "Test2");
    // c.add_child(TaskItem::new("C", "C2"));
    // c.get_task(0)
    //     .unwrap()
    //     .add_child(TaskItem::new("Inside C", "Inside C2"));
    // c.get_task(0)
    //     .unwrap()
    //     .get_task(0)
    //     .unwrap()
    //     .add_child(TaskItem::new("Inside Inside C", "Inside Inside C2"));

    // c.add_child(TaskItem::new("Second", "Debug"));
    // tl.add_task(c);

    // // tl.add_task(c);
    // // println!("{:#?}", tl);
    // // tl.change_task(0, TaskItem::new("Test new", "Test2 new"), true);

    // println!("{}", tl.format_tasks(0, 4));
    // // println!("{:#?}", c);
    let a = (1, "A");
}
